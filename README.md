Braseidon Laravel Integration for Stem Bot
===================

### Installation

1. `npm install git://github.com/braseidon/braseidon-stem-laravel.git`
2. Add `"braseidon-stem-laravel": {}` to plugins.json

### Commands

- `example` This is just an example